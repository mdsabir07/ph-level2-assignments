# 📝 Blog Post 1: TypeScript-এ Interface আর Type এর মধ্যে পার্থক্য

TypeScript-এ আমরা ডেটার স্ট্রাকচার বা আকৃতি (shape) নির্ধারণ করতে `interface` এবং `type alias` — দুইটাই ব্যবহার করতে পারি। দেখতে একই রকম মনে হলেও এদের মাঝে কিছু গুরুত্বপূর্ণ পার্থক্য আছে।

## 🔹 1. **Extend / Inherit করার ক্ষেত্রে পার্থক্য**

- **Interface** : Multiple inheritance সাপোর্ট করে
    ```typescript
    interface A { a: number; }
    interface B { b: number; }

    interface C extends A, B {
      c: number;
    }
    ```

- **Type** : Intersection দিয়ে inheritance করতে হয়
    ```typescript
    type A = { a: number };
    type B = { b: number };

    type C = A & B & { c: number };
    ```

---

## 🔹 2. **Interface পুনরায় declare করা যায় (Declaration Merging)**

- **Interface** এ পুনরায় declaration করা যায়:
    ```typescript
    interface User {
      name: string;
    }

    interface User {
      age: number;
    }

    // ফলাফল:
    const person: User = { name: "Rakib", age: 22 };
    ```

- **Type** এ এটা করা যায় না:
    ```typescript
    type User = { name: string };
    type User = { age: number }; // ❌ Error
    ```

---

## 🔹 3. **Interface মূলত Object Shape এর জন্য — Type বেশি generalized**

- **Interface**:
    - object
    - class
    - function shape এর জন্য বেশি ব্যবহার হয়।

- **Type**:
    - string, number, union, tuple, literal সব define করা যায়।
    ```typescript
    type ID = string | number;
    type Status = "active" | "inactive";
    type Pair = [number, number];
    ```

**Interface দিয়ে এগুলো করা যায় না।**

---

## 🔹 4. **Best Practice**

- **Object structure** → `Interface` recommended
- **Complex type combination** → `Type` better

---

## ✔ **উপসংহার**

`Interface` ও `Type` অনেকটা একই কাজ করলেও, 
- `Interface` বেশি **object-centric** এবং 
- `Type` বেশি **flexible**। 

বড় project-এ `interface` ব্যবহারে readability ও structure maintain করা সহজ হয়।

---

# 📝 Blog Post 2: TypeScript-এ any, unknown, এবং never এর পার্থক্য

TypeScript-এর অন্যতম confusing তিনটি টাইপ হলো `any`, `unknown`, এবং `never`। এগুলোকে বুঝতে পারলে TypeScript ভালোভাবে ব্যবহার করা অনেক সহজ হয়ে যায়।

## 🔹 1. **any — সবচেয়ে dangerous টাইপ 😅**

`any` মানে হলো: “TypeScript, তুমি চুপ থাকো… আমিই সব সামলে নেব।”

```typescript
let data: any = 10;
data = "hello";
data = false;


এখানে, data টাইপ any হওয়ায় আমরা যে কোন ধরনের ডেটা সেট করতে পারি এবং কোন error ছাড়াই কাজ করতে পারি। আপনি যেকোনো অপারেশন চালাতে পারবেন, এবং TypeScript কোন ধরনের type mismatch বা error দেখাবে না। এটি খুবই বিপজ্জনক, কারণ আপনি type safety হারিয়ে ফেলেন এবং এটি বড় প্রজেক্টে সমস্যা সৃষ্টি করতে পারে।

তবে, any ব্যবহারের সময় সতর্ক থাকা উচিত, এবং এটি শুধুমাত্র অত্যন্ত প্রয়োজন হলে ব্যবহার করা উচিত।

##🔹 2. **unknown — নিরাপদ any**

যদি any এর মতোই কিছু দরকার হয়, তবে unknown টাইপ একটি নিরাপদ বিকল্প। unknown খুবই কাছাকাছি any এর মতো, তবে এটি type checking এর জন্য বাধ্য করে। এর মানে হলো, unknown ব্যবহারের আগে আপনাকে অবশ্যই টাইপ চেক করতে হবে।

উদাহরণ:
let input: unknown;

input = "hello";
input = 12;

// সরাসরি ব্যবহার করা যাবে না
input.toUpperCase(); // ❌ Error

এখানে input কে unknown টাইপ দেওয়া হয়েছে, এবং আপনি সরাসরি toUpperCase() চালাতে পারবেন না। আপনি যদি এই ডেটার উপর কোনো অপারেশন করতে চান, তাহলে অবশ্যই প্রথমে টাইপ চেক করতে হবে:
if (typeof input === "string") {
  input.toUpperCase(); // now valid
}

এভাবে, unknown ব্যবহার করে আপনি type safety বজায় রাখতে পারেন এবং ডেটার সঠিক টাইপ যাচাই করার পরেই কোনো অপারেশন চালাতে পারবেন।

unknown হল any এর একটি নিরাপদ বিকল্প এবং এটি ব্যবহার করার সময় টাইপ চেক করা আবশ্যক।

🔹 3. never — এমন টাইপ যা কখনো ঘটে না

never টাইপটি এমন কোনো value কে নির্দেশ করে যা কখনোই আসবে না। এটি বিশেষভাবে error handling বা infinite loop এর মতো ক্ষেত্রে ব্যবহৃত হয়। যখন আপনি নিশ্চিত যে কোনো ফাংশন কখনোও কোনো value return করবে না, তখন আপনি never টাইপ ব্যবহার করেন।

উদাহরণ ১: Function যা error ছুঁড়ে ফেলে এবং কখনো return করে না
function crash(message: string): never {
  throw new Error(message);
}


এই ফাংশনটি কখনোই কোনো value return করবে না, কারণ এটি error ছুঁড়ে ফেলে এবং execution থামিয়ে দেয়। এখানে never টাইপ এর মাধ্যমে আপনি TypeScript-কে বলছেন যে, এই ফাংশন থেকে কখনোও কোনো valid value পাওয়া যাবে না।

উদাহরণ ২: Infinite loop
function loopForever(): never {
  while (true) {}
}


এটি একটি ইনফিনিট লুপ যা কখনোই শেষ হবে না। এভাবে never টাইপ ব্যবহার করে আপনি বুঝাচ্ছেন যে, এই ফাংশনের execution কখনোই শেষ হবে না এবং কোনো value return করবে না।

উদাহরণ ৩: Exhaustive checking — খুবই গুরুত্বপূর্ণ
type Shape = "circle" | "square";

function draw(shape: Shape) {
  if (shape === "circle") {
    // Drawing circle...
  } else if (shape === "square") {
    // Drawing square...
  } else {
    const _exhaustive: never = shape; // ❌ যদি নতুন টাইপ যোগ করা হয় error দিবে
  }
}

এখানে, আমরা Shape টাইপের জন্য exhaustive checking করছি। যদি নতুন কোনো shape টাইপ যোগ করা হয়, তবে TypeScript error দিবে, কারণ _exhaustive এর টাইপ never হওয়ায় এটি কখনোই একটি নতুন টাইপ গ্রহণ করতে পারবে না। এটি খুবই গুরুত্বপূর্ণ যখন আপনি নিশ্চিত হতে চান যে, আপনার কোডে সব ধরণের input হ্যান্ডেল হচ্ছে।

never মূলত impossible বা unexpected অবস্থাগুলো ধরতে ব্যবহৃত হয়।

✔ উপসংহার

TypeScript-এ any, unknown, এবং never তিনটি ভিন্ন টাইপ রয়েছে, এবং তাদের ব্যবহারের ক্ষেত্রে প্রতিটি টাইপের নিজস্ব সুবিধা ও দুর্বলতা আছে।

টাইপ	কাজ
any	TypeScript বন্ধ হয়ে যায় → যে কোনো কিছু করা যায়
unknown	নিরাপদ any → আগে type check করতে হবে
never	কখনোই ঘটবে না এমন value এর জন্য

any ব্যবহার করুন শুধুমাত্র যখন আপনি নিশ্চিত না যে কোন টাইপ ব্যবহার করবেন এবং টাইপ সেফটি গুরুত্বপূর্ণ নয়।
unknown ব্যবহার করুন যখন আপনি নিরাপদভাবে টাইপ চেক করতে চান এবং টাইপ সেফটি বজায় রাখতে চান।
never ব্যবহার করুন যখন আপনি নিশ্চিত যে কোনো ফাংশন কখনোই কোনো value return করবে না।

TypeScript-এ type safety বজায় রাখতে হলে any কম ব্যবহার করুন, আর unknown ও never স্মার্টভাবে ব্যবহার করুন।